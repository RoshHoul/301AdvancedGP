For the application to work the server must be ran first and after it is definitely running then the clients can connect to it.

The engine is not finished and can run only one client at a time. It still exchanges drawing information between the client and the server.